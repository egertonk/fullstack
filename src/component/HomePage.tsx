import React from "react";
import { Modal } from "react-bootstrap";

type props = {
  show: boolean;
  onHide: () => void;
  MajorDuties: string[];
  title: string;
};

export const HomePage = () => {
  return <>Egerton During</>;
};
